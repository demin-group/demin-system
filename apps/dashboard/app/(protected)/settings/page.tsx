import { PlaceholderPanel } from "@/components/placeholder-panel";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return (
    <PlaceholderPanel
      title="Settings"
      phase="Fase 1"
      description="Buzones, palabras gatillo de auto-pausa, modo HITL/autónomo, configuración del sistema."
    />
  );
}
