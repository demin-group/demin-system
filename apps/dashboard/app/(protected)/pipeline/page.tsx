import { PlaceholderPanel } from "@/components/placeholder-panel";

export const metadata = { title: "Pipeline" };

export default function PipelinePage() {
  return (
    <PlaceholderPanel
      title="Pipeline"
      phase="Fase 1"
      description="Vista del funnel de outreach: empresas filtradas, contactos enriquecidos, secuencias en curso."
    />
  );
}
