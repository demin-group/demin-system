import { PlaceholderPanel } from "@/components/placeholder-panel";

export const metadata = { title: "Metrics" };

export default function MetricsPage() {
  return (
    <PlaceholderPanel
      title="Metrics"
      phase="Fase 1"
      description="KPIs de la operativa: enviados, abiertos, respondidos, tasa de respuesta, conversión."
    />
  );
}
